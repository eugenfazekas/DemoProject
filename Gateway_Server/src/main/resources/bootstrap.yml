spring:
    application:
      name: gateway-server 
    profiles:
      active: ${SPRING_DEMO_PROJECT_PROFILE}
    cloud:
      config: 
        uri: http://localhost:8888

management:  
  endpoints:   
    web:
     exposure:
        include: health

